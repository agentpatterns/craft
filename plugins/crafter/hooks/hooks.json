{
  "description": "Crafter plugin hooks for plan-mode-native RPI workflow",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "EnterPlanMode",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Check .claude/scratch/ for any research artifact (*-research.md). If one exists and is relevant to the current task, mention it so the planning phase can build on it."
          }
        ]
      }
    ]
  }
}
